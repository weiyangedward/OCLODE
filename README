To compile:
>cd makedir
>rm -rf *
>cmake ..
>make

(If compile under mac OS, please change '-std=c++0x' to '-std=c++11' in 'CMakeLists.txt')

To run:
>sh run.sh

(Please do not use '-n' so that the runs will be finished sooner)